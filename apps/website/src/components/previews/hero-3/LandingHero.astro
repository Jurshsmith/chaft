---
import ProductConstellationLandingHero from "../../landing/ProductConstellationLandingHero.astro";

interface Props {
  hasPublishedDownloads: boolean;
}

const { hasPublishedDownloads } = Astro.props;
---

<ProductConstellationLandingHero {hasPublishedDownloads} heroId="hero-3" />
