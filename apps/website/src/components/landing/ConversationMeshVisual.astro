<div
  class="conversation-mesh"
  role="img"
  aria-label="A small team conversation syncing directly and securely between three trusted devices"
>
  <div class="conversation-mesh__board" aria-hidden="true">
    <div class="conversation-mesh__header">
      <div class="conversation-mesh__room">
        <span class="conversation-mesh__mark">
          <span></span>
          <span></span>
        </span>
        <span>Team room</span>
      </div>

      <span class="conversation-mesh__mode">
        <svg viewBox="0 0 16 16" aria-hidden="true">
          <path d="M8 1.8 13 3.7v3.8c0 3.1-2 5.5-5 6.7-3-1.2-5-3.6-5-6.7V3.7L8 1.8Z"></path>
          <path d="m5.8 7.8 1.4 1.4 3-3"></path>
        </svg>
        Direct sync
      </span>
    </div>

    <div class="conversation-mesh__canvas">
      <svg class="conversation-mesh__routes" viewBox="0 0 480 420">
        <defs>
          <linearGradient id="hero-four-route-a" x1="0" y1="0" x2="1" y2="1">
            <stop offset="0" stop-color="#83cce6"></stop>
            <stop offset="1" stop-color="#6de0ba"></stop>
          </linearGradient>
          <linearGradient id="hero-four-route-b" x1="1" y1="0" x2="0" y2="1">
            <stop offset="0" stop-color="#69d9b4"></stop>
            <stop offset="1" stop-color="#8fbde2"></stop>
          </linearGradient>
          <filter id="hero-four-route-glow" x="-30%" y="-30%" width="160%" height="160%">
            <feGaussianBlur stdDeviation="3.5" result="blur"></feGaussianBlur>
            <feMerge>
              <feMergeNode in="blur"></feMergeNode>
              <feMergeNode in="SourceGraphic"></feMergeNode>
            </feMerge>
          </filter>
        </defs>

        <path
          class="conversation-mesh__route conversation-mesh__route--muted"
          d="M74 67C195 14 342 29 410 94"
        ></path>
        <path
          class="conversation-mesh__route conversation-mesh__route--muted"
          d="M74 67C80 245 218 371 398 346"
        ></path>
        <path
          class="conversation-mesh__route conversation-mesh__route--muted"
          d="M410 94C458 203 456 286 398 346"
        ></path>

        <path
          class="conversation-mesh__route conversation-mesh__route--active conversation-mesh__route--one"
          d="M74 67C195 14 342 29 410 94"
        ></path>
        <path
          class="conversation-mesh__route conversation-mesh__route--active conversation-mesh__route--two"
          d="M410 94C458 203 456 286 398 346"
        ></path>

        <circle class="conversation-mesh__pulse conversation-mesh__pulse--one" cx="74" cy="67" r="15"></circle>
        <circle class="conversation-mesh__pulse conversation-mesh__pulse--two" cx="410" cy="94" r="15"></circle>
        <circle class="conversation-mesh__pulse conversation-mesh__pulse--three" cx="398" cy="346" r="15"></circle>
      </svg>

      <article class="peer peer--you">
        <span class="peer__avatar peer__avatar--you">Y</span>
        <span class="peer__identity">
          <strong>You</strong>
          <span>MacBook</span>
        </span>
        <span class="peer__presence" title="Connected"></span>
      </article>

      <article class="peer peer--mira">
        <span class="peer__avatar peer__avatar--mira">M</span>
        <span class="peer__identity">
          <strong>Mira</strong>
          <span>Phone</span>
        </span>
        <span class="peer__presence" title="Connected"></span>
      </article>

      <article class="peer peer--kai">
        <span class="peer__avatar peer__avatar--kai">K</span>
        <span class="peer__identity">
          <strong>Kai</strong>
          <span>Laptop</span>
        </span>
        <span class="peer__presence" title="Connected"></span>
      </article>

      <div class="message message--outgoing">
        <span class="message__speaker">You</span>
        <p>The new build is ready.</p>
        <span class="message__meta">
          09:41
          <svg viewBox="0 0 18 12" aria-hidden="true">
            <path d="m1 6 3.2 3L10.5 2"></path>
            <path d="m7.1 8.8.6.6L16.7 1"></path>
          </svg>
        </span>
      </div>

      <div class="message message--reply">
        <span class="message__speaker">Mira</span>
        <p>Got it — checking it now.</p>
        <span class="message__meta">09:42 · delivered directly</span>
      </div>

      <span class="conversation-mesh__packet conversation-mesh__packet--one">
        <svg viewBox="0 0 18 18" aria-hidden="true">
          <path d="M3 4.2h12v7.7H8.2L5 14.6v-2.7H3V4.2Z"></path>
        </svg>
      </span>
      <span class="conversation-mesh__packet conversation-mesh__packet--two">
        <svg viewBox="0 0 18 18" aria-hidden="true">
          <path d="M3 4.2h12v7.7H8.2L5 14.6v-2.7H3V4.2Z"></path>
        </svg>
      </span>
    </div>

    <div class="conversation-mesh__footer">
      <span>
        <i></i>
        3 peers connected
      </span>
      <span>Encrypted before replication</span>
    </div>
  </div>

  <span class="conversation-mesh__note conversation-mesh__note--direct">
    <svg viewBox="0 0 18 18" aria-hidden="true">
      <circle cx="5" cy="9" r="2"></circle>
      <circle cx="13" cy="5" r="2"></circle>
      <circle cx="13" cy="13" r="2"></circle>
      <path d="m6.8 8 4.4-2M6.8 10l4.4 2"></path>
    </svg>
    Device to device
  </span>
</div>

<style>
  .conversation-mesh {
    --mesh-aqua: #72dfbd;
    --mesh-blue: #8ac9e3;
    --mesh-coral: #ed7358;
    position: relative;
    display: grid;
    width: min(100%, 34rem);
    min-height: 38rem;
    padding-bottom: 3.6rem;
    align-content: start;
    justify-items: center;
    justify-self: end;
    font-family: var(--font-ui);
    isolation: isolate;
  }

  .conversation-mesh::before,
  .conversation-mesh::after {
    position: absolute;
    border-radius: 50%;
    content: "";
    filter: blur(8px);
    pointer-events: none;
  }

  .conversation-mesh::before {
    top: 3%;
    right: -12%;
    width: 22rem;
    height: 22rem;
    background: rgb(86 204 174 / 24%);
    box-shadow: 0 0 7rem rgb(108 191 218 / 22%);
  }

  .conversation-mesh::after {
    bottom: 0;
    left: -3%;
    width: 15rem;
    height: 15rem;
    background: rgb(123 183 220 / 19%);
  }

  .conversation-mesh__board {
    position: relative;
    z-index: 1;
    width: min(100%, 31rem);
    overflow: hidden;
    border: 1px solid rgb(255 253 247 / 16%);
    border-radius: 1.75rem;
    background:
      radial-gradient(circle at 92% 7%, rgb(104 219 184 / 22%), transparent 34%),
      radial-gradient(circle at 3% 87%, rgb(114 174 213 / 18%), transparent 38%),
      linear-gradient(145deg, #1b4036 0%, #122b25 54%, #10252b 100%);
    box-shadow:
      0 2.4rem 5rem rgb(23 33 31 / 22%),
      0 0 0 0.5rem rgb(255 253 247 / 46%),
      inset 0 1px 0 rgb(255 255 255 / 11%);
    color: #fffdf7;
  }

  .conversation-mesh__board::before {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(rgb(255 255 255 / 3.8%) 1px, transparent 1px),
      linear-gradient(90deg, rgb(255 255 255 / 3.8%) 1px, transparent 1px);
    background-size: 28px 28px;
    content: "";
    mask-image: linear-gradient(180deg, #000, transparent 80%);
    pointer-events: none;
  }

  .conversation-mesh__header,
  .conversation-mesh__footer {
    position: relative;
    z-index: 2;
    display: flex;
    gap: 1rem;
    align-items: center;
    justify-content: space-between;
  }

  .conversation-mesh__header {
    min-height: 4.25rem;
    padding: 0.9rem 1.15rem;
    border-bottom: 1px solid rgb(255 253 247 / 11%);
    background: rgb(8 28 23 / 20%);
  }

  .conversation-mesh__room {
    display: flex;
    gap: 0.65rem;
    align-items: center;
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: -0.01em;
  }

  .conversation-mesh__mark {
    position: relative;
    display: block;
    width: 1.5rem;
    height: 1.5rem;
    border: 1px solid rgb(255 255 255 / 18%);
    border-radius: 0.5rem;
    background: rgb(255 255 255 / 8%);
  }

  .conversation-mesh__mark span {
    position: absolute;
    width: 0.48rem;
    height: 0.34rem;
    border-radius: 0.12rem;
  }

  .conversation-mesh__mark span:first-child {
    top: 0.4rem;
    left: 0.35rem;
    background: var(--mesh-blue);
  }

  .conversation-mesh__mark span:last-child {
    right: 0.34rem;
    bottom: 0.38rem;
    background: var(--mesh-aqua);
  }

  .conversation-mesh__mode {
    display: inline-flex;
    gap: 0.38rem;
    align-items: center;
    padding: 0.32rem 0.55rem;
    border: 1px solid rgb(114 223 189 / 20%);
    border-radius: 999px;
    background: rgb(114 223 189 / 10%);
    color: #b9f2df;
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.03em;
    white-space: nowrap;
  }

  .conversation-mesh__mode svg {
    width: 0.85rem;
    height: 0.85rem;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.5;
  }

  .conversation-mesh__canvas {
    position: relative;
    z-index: 1;
    min-height: 26rem;
  }

  .conversation-mesh__routes {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    overflow: visible;
  }

  .conversation-mesh__route {
    fill: none;
    stroke-linecap: round;
  }

  .conversation-mesh__route--muted {
    stroke: rgb(193 230 220 / 17%);
    stroke-dasharray: 3 9;
    stroke-width: 1.4;
  }

  .conversation-mesh__route--active {
    filter: url("#hero-four-route-glow");
    opacity: 0.78;
    stroke-dasharray: 5 18;
    stroke-width: 1.6;
  }

  .conversation-mesh__route--one {
    animation: route-flow 9s linear infinite;
    stroke: url("#hero-four-route-a");
  }

  .conversation-mesh__route--two {
    animation: route-flow 9s 1.3s linear infinite;
    stroke: url("#hero-four-route-b");
  }

  .conversation-mesh__pulse {
    fill: rgb(110 222 187 / 8%);
    stroke: rgb(110 222 187 / 24%);
    stroke-width: 1;
    transform-box: fill-box;
    transform-origin: center;
    animation: peer-pulse 4.8s ease-out infinite;
  }

  .conversation-mesh__pulse--two {
    animation-delay: 1.1s;
  }

  .conversation-mesh__pulse--three {
    animation-delay: 2.2s;
  }

  .peer {
    position: absolute;
    z-index: 3;
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 0.56rem;
    width: 8.35rem;
    align-items: center;
    padding: 0.52rem 0.62rem;
    border: 1px solid rgb(255 253 247 / 15%);
    border-radius: 0.82rem;
    background: rgb(13 38 32 / 82%);
    box-shadow:
      0 0.8rem 1.8rem rgb(2 13 10 / 25%),
      inset 0 1px 0 rgb(255 255 255 / 8%);
    backdrop-filter: blur(12px);
  }

  .peer--you {
    top: 1.15rem;
    left: 0.65rem;
  }

  .peer--mira {
    top: 2.8rem;
    right: 0.5rem;
  }

  .peer--kai {
    right: 1.2rem;
    bottom: 1.15rem;
  }

  .peer__avatar {
    display: grid;
    width: 1.85rem;
    height: 1.85rem;
    border-radius: 0.62rem;
    color: #10241f;
    font-size: 0.7rem;
    font-weight: 700;
    place-items: center;
  }

  .peer__avatar--you {
    background: linear-gradient(145deg, #9bd9ed, #6bbbd9);
  }

  .peer__avatar--mira {
    background: linear-gradient(145deg, #9bedcf, #62d3ad);
  }

  .peer__avatar--kai {
    background: linear-gradient(145deg, #f7b09f, #ea7259);
  }

  .peer__identity {
    display: grid;
    line-height: 1.15;
  }

  .peer__identity strong {
    color: #fffdf7;
    font-size: 0.72rem;
    font-weight: 600;
  }

  .peer__identity span {
    margin-top: 0.18rem;
    color: rgb(229 241 236 / 55%);
    font-size: 0.59rem;
  }

  .peer__presence {
    width: 0.4rem;
    height: 0.4rem;
    border: 1px solid rgb(255 255 255 / 36%);
    border-radius: 50%;
    background: var(--mesh-aqua);
    box-shadow: 0 0 0.65rem rgb(114 223 189 / 82%);
  }

  .message {
    position: absolute;
    z-index: 4;
    width: min(14.5rem, 58%);
    padding: 0.7rem 0.78rem 0.62rem;
    border-radius: 1rem;
    box-shadow: 0 1rem 2.2rem rgb(2 13 10 / 22%);
  }

  .message--outgoing {
    top: 8.7rem;
    left: 2.35rem;
    border-top-left-radius: 0.3rem;
    background: linear-gradient(145deg, #8ce6ca, #66d4b1);
    color: #102720;
  }

  .message--reply {
    top: 16.7rem;
    right: 2.3rem;
    border: 1px solid rgb(255 253 247 / 14%);
    border-top-right-radius: 0.3rem;
    background: rgb(255 253 247 / 11%);
    color: #fffdf7;
    backdrop-filter: blur(15px);
  }

  .message__speaker {
    display: block;
    margin-bottom: 0.16rem;
    color: currentColor;
    font-size: 0.59rem;
    font-weight: 700;
    letter-spacing: 0.055em;
    text-transform: uppercase;
    opacity: 0.68;
  }

  .message p {
    margin: 0;
    color: currentColor;
    font-family: var(--font-body);
    font-size: 0.83rem;
    font-weight: 500;
    letter-spacing: 0.005em;
    line-height: 1.45;
  }

  .message__meta {
    display: flex;
    gap: 0.28rem;
    align-items: center;
    justify-content: flex-end;
    margin-top: 0.32rem;
    color: currentColor;
    font-size: 0.55rem;
    opacity: 0.58;
  }

  .message__meta svg {
    width: 0.9rem;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.5;
  }

  .conversation-mesh__packet {
    position: absolute;
    z-index: 2;
    display: grid;
    width: 1.75rem;
    height: 1.75rem;
    border: 1px solid rgb(140 220 198 / 31%);
    border-radius: 50%;
    background: #173b32;
    box-shadow:
      0 0 0 0.34rem rgb(101 213 178 / 9%),
      0 0 1.3rem rgb(101 213 178 / 28%);
    color: var(--mesh-aqua);
    place-items: center;
  }

  .conversation-mesh__packet svg {
    width: 0.88rem;
    fill: none;
    stroke: currentColor;
    stroke-linejoin: round;
    stroke-width: 1.45;
  }

  .conversation-mesh__packet--one {
    top: 5.3rem;
    left: 52%;
    animation: packet-float 5.8s ease-in-out infinite;
  }

  .conversation-mesh__packet--two {
    right: 0.65rem;
    bottom: 7.8rem;
    animation: packet-float 5.8s 1.8s ease-in-out infinite;
  }

  .conversation-mesh__footer {
    min-height: 3.7rem;
    padding: 0.8rem 1.15rem;
    border-top: 1px solid rgb(255 253 247 / 10%);
    background: rgb(7 22 18 / 22%);
    color: rgb(231 244 238 / 62%);
    font-size: 0.63rem;
    letter-spacing: 0.01em;
  }

  .conversation-mesh__footer span:first-child {
    display: flex;
    gap: 0.42rem;
    align-items: center;
    color: #c4f3e3;
  }

  .conversation-mesh__footer i {
    width: 0.42rem;
    height: 0.42rem;
    border-radius: 50%;
    background: var(--mesh-aqua);
    box-shadow: 0 0 0.65rem rgb(114 223 189 / 74%);
  }

  .conversation-mesh__note {
    position: absolute;
    z-index: 3;
    display: inline-flex;
    gap: 0.5rem;
    align-items: center;
    padding: 0.55rem 0.72rem;
    border: 1px solid rgb(23 33 31 / 10%);
    border-radius: 0.75rem;
    background: rgb(255 253 247 / 89%);
    box-shadow:
      0 1rem 2.6rem rgb(23 33 31 / 15%),
      inset 0 1px 0 #fff;
    color: var(--text-strong);
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.015em;
    backdrop-filter: blur(12px);
  }

  .conversation-mesh__note svg {
    width: 1rem;
    fill: var(--mesh-aqua);
    stroke: #077054;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.2;
  }

  .conversation-mesh__note--direct {
    right: -0.25rem;
    bottom: 0.45rem;
  }

  @keyframes route-flow {
    to {
      stroke-dashoffset: -92;
    }
  }

  @keyframes peer-pulse {
    0%,
    55% {
      opacity: 0.75;
      transform: scale(0.42);
    }

    90%,
    100% {
      opacity: 0;
      transform: scale(1.65);
    }
  }

  @keyframes packet-float {
    0%,
    100% {
      transform: translate3d(0, 0, 0);
    }

    50% {
      transform: translate3d(0.2rem, -0.35rem, 0);
    }
  }

  @media (max-width: 1050px) {
    .conversation-mesh {
      width: min(100%, 38rem);
      justify-self: center;
    }
  }

  @media (max-width: 540px) {
    .conversation-mesh {
      min-height: 33.75rem;
      padding-bottom: 3.25rem;
    }

    .conversation-mesh__board {
      border-radius: 1.35rem;
    }

    .conversation-mesh__canvas {
      min-height: 22rem;
    }

    .conversation-mesh__routes {
      transform: scaleY(0.84);
      transform-origin: top center;
    }

    .peer {
      width: 7.35rem;
      padding: 0.45rem 0.5rem;
    }

    .peer__avatar {
      width: 1.55rem;
      height: 1.55rem;
    }

    .peer--mira {
      top: 2.1rem;
    }

    .peer--kai {
      right: 0.55rem;
      bottom: 0.8rem;
    }

    .message {
      width: min(13rem, 65%);
    }

    .message--outgoing {
      top: 7.6rem;
      left: 1.15rem;
    }

    .message--reply {
      top: 14rem;
      right: 1.1rem;
    }

    .conversation-mesh__packet--one {
      top: 4.8rem;
    }

    .conversation-mesh__packet--two {
      bottom: 5.9rem;
    }

    .conversation-mesh__footer {
      flex-direction: column;
      gap: 0.25rem;
      align-items: flex-start;
    }

    .conversation-mesh__note--direct {
      right: 0.3rem;
      bottom: 0.25rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .conversation-mesh__route--active,
    .conversation-mesh__pulse,
    .conversation-mesh__packet {
      animation: none;
    }

    .conversation-mesh__pulse {
      opacity: 0.45;
      transform: scale(0.72);
    }
  }
</style>
