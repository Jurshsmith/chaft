---
import ConversationMeshLandingHero from "./ConversationMeshLandingHero.astro";

interface Props {
  hasPublishedDownloads: boolean;
}

const { hasPublishedDownloads } = Astro.props;
---

<ConversationMeshLandingHero {hasPublishedDownloads} heroId="baseline" />
