[package]
name = "chaft-benchmarks"
edition.workspace = true
license.workspace = true
publish = false
repository.workspace = true
rust-version.workspace = true
version.workspace = true

[[bench]]
name = "hot_paths"
harness = false

[dependencies]
chaft-ffi = { path = "../chaft-ffi" }
chaft-media = { path = "../chaft-media" }
chaft-net = { path = "../chaft-net" }
chaft-net-direct = { path = "../chaft-net-direct" }
chaft-runtime = { path = "../chaft-runtime" }
chaft-store = { path = "../chaft-store" }
chaft-types = { path = "../chaft-types" }
criterion = "0.5"
tempfile.workspace = true
tokio.workspace = true
